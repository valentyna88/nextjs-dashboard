import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Use it for PPR that is experimental now and not stable yet
  // experimental: {
  //   ppr: "incremental",
  // },
};

export default nextConfig;
